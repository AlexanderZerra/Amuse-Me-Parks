import React, { Component } from 'react'

export default class RollerCoasterDetails extends Component {
  render() {
    console.log(this.props)
    // const rollercoaster = this.props.selectedRollercoaster

    return (
      <div>
        {/* <img src={rollercoaster.image} alt={rollercoaster.name} />
        <h2 className="rollercoastername">{rollercoaster.name}</h2>
        <h3 className="rollercoasterdescription">
          {rollercoaster.description}
        </h3>
        <h4 className="rollercoasterheight">{rollercoaster.height}</h4>
        <p className="rollercoasterlength">{rollercoaster.length} </p>
        <p className="rollercoasterheight">{rollercoaster.topSpeed}</p> */}
      </div>
    )
  }
}
